<div class="col-md-4">
  <%= link_to image_tag(recipe.image.url(:large)), recipe.image.url %>
  <p><%= link_to recipe.name, recipe%></p>
  <p>Tempo de preparo: <%= recipe.cooking_time_formatted %></p>
</div>
